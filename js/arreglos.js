let numbers = [];
let even = [];
let odd = [];

for (let i; i < 50; i++)
    numbers.push(i + 1);

numbers.forEach((number) => {
    if (number % 2 === 0) {
        even.push(number)
    } else {
        odd.push(odd)
    }
})